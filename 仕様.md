# Real Poker Game Engine 実装仕様書（現行コード同期版）

この仕様書は、`e:\projects\dev\poker` の**現在の実装を再現できる**ことを目的とした、単一の正本仕様である。

## 1. 目的

- 物理テーブルでのテキサスホールデム進行をデジタル支援する。
- カード配布・役判定は行わない。
- 対象は以下:
  - ブラインド投稿
  - アクション検証（fold/check/call/bet/raise）
  - ポット/ターン/ラウンド進行
  - ショーダウン後処理
  - ローカル永続化（Dexie）

## 2. 技術スタック

- Next.js 15 (App Router, `src/` 構成)
- React 19 + TypeScript
- Tailwind CSS v4
- shadcn/ui
- Zustand + Immer
- Dexie (IndexedDB)
- Framer Motion
- Vitest

## 3. ディレクトリ構成（現行）

```text
src/
├── app/
│   ├── layout.tsx
│   ├── page.tsx
│   ├── manifest.ts
│   └── globals.css
├── components/ui/...
├── features/
│   ├── players/player-setup.tsx
│   ├── betting/betting-controls.tsx
│   ├── turn/turn-indicator.tsx
│   └── game/{poker-table,showdown-panel,game-log}.tsx
├── hooks/use-hydrate-game.ts
├── lib/
│   ├── poker/{helpers,game,pots,index}.ts
│   ├── storage/{game-db,index}.ts
│   └── utils.ts
├── stores/game-store.ts
└── types/poker.ts
```

## 4. 型定義（`src/types/poker.ts`）

```ts
export type Round = "Pre-flop" | "Flop" | "Turn" | "River" | "Showdown";
export type PlayerStatus = "active" | "folded" | "all-in" | "out";

export interface Player {
  id: string;
  name: string;
  stack: number;
  bet: number;
  totalContributed: number;
  status: PlayerStatus;
  position: "D" | "SB" | "BB" | null;
}

export interface PokerState {
  players: Player[];
  dealerIndex: number;
  currentPlayerIndex: number;
  pot: number;
  currentBet: number;
  minRaise: number;
  round: Round;
  smallBlind: number;
  bigBlind: number;
  lastAggressorIndex: number;
  history: string[];
  actedThisStreet: boolean[];
  handInProgress: boolean;
}

export type PokerAction =
  | { type: "fold" }
  | { type: "check" }
  | { type: "call" }
  | { type: "bet"; amount: number }
  | { type: "raise"; toAmount: number };
```

## 5. ポーカーエンジン仕様（`src/lib/poker`）

### 5.1 基本方針

- エンジンは UI 非依存の純ロジック。
- 主関数:
  - `createEmptyPokerState()`
  - `startHand()`
  - `validateAction()`
  - `applyAction()`
  - `distributePotAndEndHand()`
- 戻り値は `ApplyResult`（`ok/error`）。

### 5.2 ブラインド・席順

- `assignBlindPositions()`:
  - HU: dealer=SB, 相手=BB
  - 3人以上: D→SB→BB
- `startHand()`:
  - 必要人数: `out` 以外が2人以上
  - 2ハンド目以降は `dealerIndex` を回転
  - SB/BBを `payFromStack` で投稿

### 5.3 アクション検証

- `validateAction()` で以下を検証:
  - ハンド進行中
  - 対象席が有効
  - `currentPlayerIndex` 一致
  - `stack > 0`（オールインはアクション不可）
  - check/call/bet/raise の各条件
- `raise` は `minTotal = currentBet + minRaise` を下限。

### 5.4 ラウンド完了判定

- `actedThisStreet` と `needsVoluntaryMark()` で「行動済み」を管理。
- `allFacingBetsMatched()` + `voluntaryComplete()` でクローズ判定。
- クローズ時は `advanceStreet()` で次ストリートへ。

### 5.5 0スタック/オールイン処理

- `stack=0` は `all-in` 扱い。
- 自発行動不要プレイヤーはターン候補から除外。
- `settleNoActionFlow()`:
  - 行動可能者がいなければ自動でストリートを進める。
  - 全員オールイン等で進行停止しないようにする。

### 5.6 ショーダウン

- `Showdown` では実カードで勝者を決め、`showdownWinner()` を呼ぶ。
- 現行実装は `showdownWinner()` 実行後に自動で次ハンド開始を試みる。

## 6. ストア仕様（`src/stores/game-store.ts`）

### 6.1 状態

- `poker: PokerState`
- `undoStack: PokerState[]`
- `hydrated: boolean`
- `initialStack: number`

### 6.2 永続化

- Dexieでスナップショット1件保存。
- `queuePersist`（debounce 400ms）で保存。
- `hydrate()` でロードし `ensureDerivedArrays()` 適用。

### 6.3 アクション

- `setBlinds`, `setInitialStack`, `addPlayer`, `removePlayer`
- `startNewHand`, `submitAction`, `showdownWinner`, `showdownChop`, `undoLastAction`, `resetSession`
- `logBoardCards`, `logShowdownCards`, `logNote`
- `showdownWinner` / `showdownChop` はポット分配後、`startHand` で自動次ハンド化。

## 7. UI/UX 仕様（現行）

- 白基調 + カジノアクセント（緑/金）
- モバイル優先
- 下部固定アクションバー
- プレイヤーカードは横スクロール
- 現在ターンはカード全体強調
- 各プレイヤー表示:
  - 名前
  - 位置（SB/BB/D）
  - 所持チップ
  - このターン投入額（`bet`）
- リセットは2段階確認ボタン

### ベッティングUI

- Fold / Check(or Call) / Bet(or Raise)
- Bet時・Raise時ともクイックボタンを表示
- call表示は支払可能額ベース:
  - 足りない場合は `オールイン <amount>`
- quickボタン:
  - `2x / 2.5x / 3x / Pot / 1/2 Pot / All-in`
  - `Pot` は現在ポット額をそのまま入力
  - 最終的な有効性は `validateAction` で判定

## 8. PWA

- `src/app/manifest.ts` で manifest 提供。
- `layout.tsx` で viewport / metadata を設定。

## 9. テスト

- `src/lib/poker/game.test.ts`
- `src/lib/poker/pots.test.ts`
- `npm test` で実行（Vitest）

## 10. 実行手順

```bash
npm install
npm run dev
```

## 11. 制約と今後

- ローカル永続化のみ（Dexie）。端末間のリアルタイム共有は行わない。
- 将来拡張候補:
  - マルチデバイス同期
  - 認証
  - 永続バックエンド
